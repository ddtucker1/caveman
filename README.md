# Wildborn

A procedurally evolving 2D survival game (HTML5 Canvas + vanilla JS).

Open `index.html` in a browser — no build step, no frameworks.

## Phase 1 (current)

Seeded world generation, chunked tilemap, movable player, camera follow.

See the in-game help overlay for controls.
